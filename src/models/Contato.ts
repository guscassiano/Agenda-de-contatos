class Contato {
  nome: string
  email: string
  numero: number
  id: number

  constructor(nome: string, email: string, numero: number, id: number) {
    this.nome = nome
    this.email = email
    this.numero = numero
    this.id = id
  }
}

export default Contato
